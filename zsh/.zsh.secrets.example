# Example private shell settings.
# Copy to ~/.zsh.secrets and fill in values that should not live in git.

# export ANTHROPIC_BASE_URL="https://example.invalid/anthropic"
# export ANTHROPIC_AUTH_TOKEN="replace-me"
# export ANTHROPIC_MODEL="replace-me"
